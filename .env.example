# Public site URL, used for absolute metadata / OpenGraph URLs.
NEXT_PUBLIC_SITE_URL=https://gitztech.com

# Optional: contact form endpoint (Formspree, Resend proxy, etc.).
# The contact form falls back to a mailto: link when this is unset.
NEXT_PUBLIC_CONTACT_ENDPOINT=
